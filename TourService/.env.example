# Replace placeholders in a local .env file.
# Pass it with docker run --env-file .env; ASP.NET Core does not load .env automatically.
MongoDbSettings__ConnectionString=mongodb://YOUR_MONGODB_HOST:27017
MongoDbSettings__DatabaseName=YOUR_DATABASE_NAME
ASPNETCORE_URLS=http://+:5281
